# 📁 نظام رفع وإدارة الملفات

نظام كامل لرفع وإدارة الملفات باستخدام Node.js, Express, MySQL

## ✨ المميزات

- ✅ رفع الملفات (حتى 16 ميجابايت)
- ✅ عرض قائمة الملفات
- ✅ تحميل الملفات
- ✅ حذف الملفات
- ✅ واجهة مستخدم عربية جميلة
- ✅ جاهز للرفع على Railway

## 🚀 التشغيل المحلي

### 1. تثبيت الحزم:
```bash
npm install
```

### 2. إعداد متغيرات البيئة:
أنشئ ملف `.env` (انظر `.env.example`):
```env
PORT=3000
DB_HOST=your_db_host
DB_PORT=23345
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
```

### 3. تشغيل السيرفر:
```bash
npm start
```

أو للتطوير مع إعادة التشغيل التلقائي:
```bash
npm run dev
```

### 4. فتح المتصفح:
افتح `http://localhost:3000`

## 🚂 الرفع على Railway

راجع ملف `RAILWAY_DEPLOY.md` للتعليمات الكاملة.

### خطوات سريعة:
1. ارفع الكود على GitHub
2. أنشئ مشروع جديد على Railway
3. اربط المستودع
4. أضف متغيرات البيئة في Railway Dashboard
5. انتظر حتى يكتمل البناء والرفع

## 📋 المتطلبات

- Node.js 14+
- MySQL Database
- npm أو yarn

## 🛠️ التقنيات المستخدمة

- **Backend:** Express.js
- **Database:** MySQL
- **File Upload:** Multer
- **Frontend:** HTML, CSS, JavaScript (Vanilla)

## 📝 ملاحظات

- الملفات تُحفظ في قاعدة البيانات (LONGBLOB)
- الحد الأقصى لحجم الملف: 16 ميجابايت
- الكود يستخدم endpoints نسبية (يعمل على أي domain)
- يدعم متغيرات البيئة للرفع على Railway

## 🔒 الأمان

⚠️ **مهم:** لا ترفع ملف `.env` على GitHub!
- استخدم Railway Variables للبيانات الحساسة
- أضف `.env` إلى `.gitignore`

## 📚 الملفات المهمة

- `server.js` - السيرفر الرئيسي
- `index.html` - الواجهة الأمامية
- `package.json` - الحزم والإعدادات
- `railway.json` - إعدادات Railway
- `RAILWAY_DEPLOY.md` - دليل الرفع على Railway

## 🐛 استكشاف الأخطاء

### مشاكل الاتصال بقاعدة البيانات:
- تحقق من متغيرات البيئة
- تأكد من أن قاعدة البيانات متاحة من الإنترنت
- تحقق من إعدادات SSL

### مشاكل رفع الملفات:
- تحقق من حجم الملف (الحد الأقصى 16 ميجابايت)
- افتح Console في المتصفح (F12) للتحقق من الأخطاء
- تأكد من أن السيرفر يعمل

## 📄 الترخيص

ISC
