# 🚂 دليل الرفع على Railway

## ✅ التغييرات التي تمت:

1. ✅ استخدام متغيرات البيئة (Environment Variables)
2. ✅ استخدام endpoints نسبية في الفرونت إند
3. ✅ إعدادات Railway جاهزة
4. ✅ دعم PORT الديناميكي

## 📋 خطوات الرفع على Railway:

### 1. إعداد المشروع على Railway:

1. اذهب إلى [Railway.app](https://railway.app)
2. أنشئ مشروع جديد (New Project)
3. اختر "Deploy from GitHub repo" أو "Empty Project"
4. إذا اخترت GitHub، اربط المستودع الخاص بك

### 2. إعداد متغيرات البيئة:

في Railway Dashboard، اذهب إلى **Variables** وأضف:

```
DB_HOST=your_database_host
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=your_database_name
NODE_ENV=production
```

**ملاحظة:** استبدل القيم بأرقامك الحقيقية من قاعدة البيانات.

**ملاحظة مهمة:** Railway يقوم بتعيين `PORT` تلقائياً، لا تحتاج لإضافته!

### 3. إعدادات البناء:

- **Build Command:** (اتركه فارغاً أو `npm install`)
- **Start Command:** `npm start`
- **Root Directory:** (اتركه فارغاً)

### 4. بعد الرفع:

1. Railway سيعطيك رابط مثل: `https://your-app.railway.app`
2. افتح الرابط في المتصفح
3. يجب أن تعمل التطبيق بشكل كامل!

## 🔧 استكشاف الأخطاء:

### إذا لم يعمل السيرفر:
- ✅ تحقق من متغيرات البيئة في Railway Dashboard
- ✅ تأكد من أن `DB_PASSWORD` صحيح
- ✅ تحقق من Logs في Railway Dashboard

### إذا لم تظهر الملفات:
- ✅ افتح Console في المتصفح (F12)
- ✅ تحقق من أن الـ API endpoints تعمل
- ✅ تأكد من اتصال قاعدة البيانات

### إذا ظهرت أخطاء CORS:
- ✅ تأكد من أن `FRONTEND_URL` مضبوط بشكل صحيح
- ✅ أو اتركه فارغاً لاستخدام `*` (للتطوير فقط)

## 📝 ملاحظات مهمة:

1. **المنفذ (PORT):** Railway يقوم بتعيينه تلقائياً عبر `process.env.PORT`
2. **قاعدة البيانات:** تأكد من أن قاعدة البيانات متاحة من الإنترنت
3. **SSL:** الكود يدعم SSL تلقائياً
4. **الملفات:** الملفات تُحفظ في قاعدة البيانات (لا حاجة لمجلد uploads)

## 🎯 الميزات الجديدة:

- ✅ يعمل على أي domain (Railway, Heroku, Vercel, إلخ)
- ✅ استخدام endpoints نسبية
- ✅ دعم متغيرات البيئة
- ✅ جاهز للإنتاج

## 🔒 الأمان:

⚠️ **مهم:** لا ترفع ملف `.env` على GitHub!
- استخدم `.env.example` كقالب
- أضف `.env` إلى `.gitignore`
- استخدم Railway Variables للبيانات الحساسة

